# Portfolio assets

Upload the following files to this folder. The React app already references these exact public paths:

- `megamall.png` — MH Mega Mall screenshot
- `portfolio.png` — previous portfolio screenshot
- `road-accident-dashboard.png` — Road Accident Dashboard screenshot
- `hussain-logo.png` — personal logo / brand mark

Do not rename the files unless the React references are updated too.

Because these are served from Vite's `public`-style root asset path, the website references them as `/assets/<filename>`.
